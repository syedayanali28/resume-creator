---
name: zhao-yanbo-tailored-resume
description: >-
  Pointer only: Zhao Yanbo resume rules live in the project rule
  resume-tailored-job-packages (globs), including hyperlinks for contact lines
  and any URLs supplied in the master. Use when browsing skills; editing
  people/Zhao-Yanbo does not require invoking this skill.
disable-model-invocation: true
---

# Zhao Yanbo tailored resume — pointer

**Do not duplicate rules here.** All instructions (tailored length, headings, **≥4 bullets per Work Experience block for Zhao**, pagination, **hyperlinks**, compile commands) are in:

**`.cursor/rules/resume-tailored-job-packages.mdc`**

That **project rule** attaches **automatically** when you open or work with files matching its `globs` (e.g. `*_resume.tex`, `*_cover-letter.tex`, and `master-resume.tex` under `people/**`).

Invoke this skill file **only** if you want a named hook in the skills list; the agent should still read and follow the **rule** above for Zhao Yanbo edits.
